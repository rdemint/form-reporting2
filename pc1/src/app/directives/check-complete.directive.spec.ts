import { CheckCompleteDirective } from './check-complete.directive';

describe('CheckCompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckCompleteDirective();
    expect(directive).toBeTruthy();
  });
});
